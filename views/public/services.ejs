<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= contactInfo ? contactInfo.company_name : 'UCC Engineering' %></title>
    <meta name="description" content="Comprehensive engineering solutions for power plants - AMC, Boiler Maintenance, ESP, APH, AHP, Hoist Operations, Shutdown Services, Overhauling">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header', { contactInfo }) %>
    
    <main>
        <section class="page-header">
            <div class="container">
                <h1><%= title %></h1>
                <p>Comprehensive engineering solutions for power plants</p>
            </div>
        </section>

        <section class="services-section">
            <div class="container">
                <div class="services-grid-detailed">
                    <% if (services && services.length > 0) { %>
                        <% services.forEach(function(service, index) { %>
                        <div class="service-card-detailed animate-fade-in" style="animation-delay: <%= index * 0.1 %>s">
                            <% if (service.image) { %>
                            <div class="service-image">
                                <img src="<%= service.image %>" alt="<%= service.name %>">
                            </div>
                            <% } %>
                            <div class="service-content">
                                <div class="service-icon">
                                    <% if (service.icon === 'wrench') { %>🔧<% } 
                                       else if (service.icon === 'fire') { %>🔥<% } 
                                       else if (service.icon === 'wind') { %>💨<% } 
                                       else if (service.icon === 'thermometer') { %>🌡️<% } 
                                       else if (service.icon === 'bolt') { %>⚡<% } 
                                       else if (service.icon === 'anchor') { %>⚓<% } 
                                       else if (service.icon === 'pause') { %>⏸️<% } 
                                       else if (service.icon === 'gear') { %>⚙️<% } 
                                       else { %>🔧<% } %>
                                </div>
                                <h3><%= service.name %></h3>
                                <p><%= service.description %></p>
                                <a href="/services/<%= service.slug %>" class="btn btn-primary btn-small">Learn More</a>
                            </div>
                        </div>
                        <% }); %>
                    <% } else { %>
                        <div class="no-services text-center">
                            <p>No services available at this time. Please check back soon.</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </section>

        <section class="cta-section">
            <div class="container">
                <h2>Need Our Services?</h2>
                <p>Contact us for a consultation and free quote</p>
                <a href="/contact" class="btn btn-light">Get in Touch</a>
            </div>
        </section>
    </main>
    
    <%- include('../partials/footer', { contactInfo }) %>
    
    <script src="/js/main.js"></script>
</body>
</html>
